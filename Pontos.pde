class Point {
  // Declarando as variáveis
  int x, y;

  // Constrtur vazio para a fruta
  Point() {
  }

  // Constructor used for snake length
  Point(int tempX, int tempY) {
    x = tempX;
    y = tempY;
  }
}
